/**
 * A function that returns a Set from array.
 * @param {Array<Object>} arrayObj - The array objects.
 * @returns {Array<Object>} array of any kind.
 */
export default function setFromArray(arrayObj) {
  return new Set(arrayObj);
}
