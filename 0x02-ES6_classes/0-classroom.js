// 0-classroom.js - Represents a classrom with a maximum capacity of students.

export default class ClassRoom {
  /**
     * Creates an instance of ClassRoom.
     * @param {number} maxStudentsSize  - The maximum capacity of the classroom.
     */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
