/* eslint-disable no-underscore-dangle */
export default class ClassRoom {
  constructor(maxStudentSize) {
    this._maxStudentSize = Number(maxStudentSize);
  }
}
/* eslint-enable no-underscore-dangle */
